<p id="notice"><%= notice %></p>

<h1>Coordinators</h1>
<%= render 'course_nav' %>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Course</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @coordinators.each do |coordinator| %>

      <tr>
        <%= gravatar_for_coor coordinator %>
        <td><%= coordinator.name %></td>
        <td><%= coordinator.email %></td>
        <td><%= link_to coordinator.course.name, coordinator.course %></td>

        <td><%= link_to 'Destroy', coordinator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Coordinator', new_coordinator_path %>
